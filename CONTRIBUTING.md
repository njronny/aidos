# 贡献指南

感谢您对 AIDOS 项目的兴趣！我们欢迎任何形式的贡献，包括但不限于代码、文档、问题反馈和功能建议。

## 行为准则

请阅读并遵守我们的 [行为准则](./CODE_OF_CONDUCT.md)（如存在），确保社区保持友好和包容。

## 如何贡献

### 1. 报告问题

如果您发现了bug或有功能建议，请通过 GitHub Issues 提交：

- 使用清晰的问题标题
- 详细描述问题或建议
- 提供复现步骤（如适用）
- 附上相关截图或日志

### 2. 提交代码

#### 开发流程

1. **Fork** 本仓库
2. **Clone** 您的 fork:
   ```bash
   git clone https://github.com/your-username/aidos.git
   cd aidos
   ```

3. **创建分支**:
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/bug-description
   ```

4. **进行开发**:
   ```bash
   # 安装依赖
   npm install
   
   # 开发模式
   npm run dev
   ```

5. **编写测试**:
   ```bash
   # 运行测试
   npm test
   
   # 运行覆盖率
   npm run test:coverage
   ```

6. **提交更改**:
   ```bash
   git add .
   git commit -m "feat: 添加新功能描述"
   ```

7. **推送并创建 Pull Request**:
   ```bash
   git push origin feature/your-feature-name
   ```

#### 提交信息规范

我们使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <description>

[optional body]

[optional footer]
```

**类型 (type)**:
- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建过程或辅助工具变动

**示例**:
```
feat(scheduler): 添加任务优先级支持
fix(gitops): 修复分支创建时的冲突问题
docs(readme): 更新安装步骤
```

### 3. 代码规范

#### 代码风格

- 使用 ESLint 进行代码检查
- 使用 Prettier 进行代码格式化
- 运行检查：
  ```bash
  npm run lint
  npm run lint:fix
  ```

#### TypeScript 规范

- 启用 strict 模式
- 所有新增代码必须类型完整
- 优先使用接口而非类型别名（公共API）

#### 测试规范

- 新功能必须包含单元测试
- 修复 bug 必须包含回归测试
- 保持测试覆盖率 > 80%

### 4. 代码审查

提交 Pull Request 后，团队成员将进行代码审查。请：

- 响应审查意见
- 保持讨论专业和建设性
- 必要时更新代码

## 开发环境设置

```bash
# 克隆项目
git clone https://github.com/your-org/aidos.git
cd aidos

# 安装依赖
npm install

# 构建
npm run build

# 运行测试
npm test
```

## 项目结构

```
aidos/
├── src/
│   ├── core/           # 核心模块
│   │   ├── analyzer/   # 需求分析器
│   │   ├── scheduler/  # 任务调度器
│   │   ├── context/    # 上下文管理
│   │   ├── gitops/     # Git操作
│   │   ├── autofix/    # 自动修复
│   │   ├── visualizer/ # 可视化
│   │   ├── notifier/   # 消息通知
│   │   └── skill/       # 技能系统
│   ├── agents/         # AI代理
│   └── index.ts        # 入口文件
├── docs/               # 文档
├── scripts/            # 脚本
└── tests/              # 测试
```

## 联系方式

- 问题反馈：GitHub Issues
- 邮件联系：team@aidos.dev

感谢您的贡献！🎉

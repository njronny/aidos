apiVersion: v1
kind: ConfigMap
metadata:
  name: aidos-config
  namespace: aidos
  labels:
    app: aidos
    component: api
data:
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  API_PORT: "80"
  API_HOST: "0.0.0.0"
  DATABASE_URL: "/app/data/aidos.db"
  LOG_FORMAT: "json"
  CORS_ORIGIN: "*"

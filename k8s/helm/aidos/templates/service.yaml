apiVersion: v1
kind: Service
metadata:
  name: {{ include "aidos.fullname" . }}
  labels:
    {{- include "aidos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aidos.selectorLabels" . | nindent 4 }}
